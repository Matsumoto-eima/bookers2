<div>
  <%= flash[:notice] %>
</div>
<p id = "notice"></p>
<div class = "row">
  <div class = "col-md-8 offset-md-1">
  <h2>Books</h2>
  <table class = "table table-hovertable-inverse">
    <thead>
      <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan = "3"></th>
    </tr></thead>
    <tbody>
<% @book.each do |book| %>
<div>
 <tr> <td><%=link_to image_tag book.user.get_profile_image(100,100), user_path(user.id) %></td>
  <td><%= link_to book.title, book_path(book.id) %></td>
 <td> <%= book.opinion %></td>
 </tr>
  </div>
<% end %>
</div>
</div>
</tbody>
</table>

<%= render 'list', book: @book %>