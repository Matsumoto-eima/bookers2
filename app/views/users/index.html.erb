<p id = "notice"></p>
<div class = "row">
  <div class = "col-md-8 offset-md-1">
    <h2>Users</h2>
    <table class = "table">
      <thead>
        <tr>
          <th>image</th>
          <th>name</th>
          <th colspan = "3"></th>
        </tr>
      </thead>
      <tbody> <% @users.each do |user| %>
<tr><td> <%=link_to image_tag book.user.get_profile_image(100,100) %></td>
<div class=text><td> <%= user.name %> </td>
<td class = "link">  <%= link_to "Show", user_path(user.id) %></td></div>
<% end %>
</tbody>
    </table>
  </div>
</div>

<%= render 'list', book: @book %>